<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle" >

    <!-- 填充的颜色 -->
    <solid android:color="@color/white" />
    <!-- 边框的颜色 ：不能和窗体背景色一样 -->
    <stroke
            android:width="1dp"
            android:color="@color/red_new1" />
    <!-- 圆角 -->
    <corners android:radius="25dp"/>

</shape>